# ✅ Проверка готовности системы к отправке рассылок

## 🔍 Что проверено

### ✅ Backend (готов)

1. **API Endpoints** - все реализованы:
   - ✅ `POST /api/campaigns` - создание кампании
   - ✅ `POST /api/campaigns/:id/start` - запуск кампании
   - ✅ `POST /api/campaigns/:id/pause` - пауза
   - ✅ `POST /api/campaigns/:id/resume` - возобновление
   - ✅ `POST /api/campaigns/:id/stop` - остановка
   - ✅ `GET /api/campaigns/:id/stats` - статистика
   - ✅ `GET /api/campaigns/:id/recipients` - получатели

2. **Worker для отправки сообщений**:
   - ✅ Реализован в `src/workers/messageWorker.ts`
   - ✅ Использует BullMQ + Redis
   - ✅ Отправляет через Meta WhatsApp API
   - ✅ Обновляет статусы получателей
   - ✅ Логирует активность

3. **Интеграция с Meta API**:
   - ✅ Отправка шаблонных сообщений
   - ✅ Построение компонентов с переменными
   - ✅ Обработка ошибок

4. **База данных**:
   - ✅ Все необходимые таблицы должны быть созданы в Supabase
   - ✅ Функции для работы с БД реализованы

### ✅ Frontend (обновлен)

1. **Создание кампаний**:
   - ✅ Переключено на бэкенд API (`useBackendCampaigns`)
   - ✅ Формат данных исправлен (rate_limit)
   - ✅ Поддержка contact_ids, contact_list_id, contact_tags

2. **Управление кампаниями**:
   - ✅ Переключено на бэкенд API
   - ✅ Кнопки start/pause/resume/stop работают через бэкенд
   - ✅ Статистика обновляется в реальном времени

3. **Список кампаний**:
   - ✅ Переключено на бэкенд API
   - ✅ Удаление через бэкенд

## ⚠️ Что нужно проверить перед отправкой

### 1. База данных Supabase

**Обязательно создать таблицы:**
- `templates` - шаблоны WhatsApp
- `campaigns` - кампании
- `campaign_recipients` - получатели кампаний
- `contacts` - контакты
- `contact_lists` - списки контактов
- `contact_list_members` - связь контактов и списков
- `activity_logs` - логи активности
- `chats` - чаты
- `messages` - сообщения
- `user_roles` - роли пользователей

**SQL схема:** см. `frontend/src/pages/Settings.tsx` (кнопка "Копировать SQL схему")

### 2. Meta WhatsApp API

**Обязательно настроить:**
- ✅ `META_ACCESS_TOKEN` - токен доступа
- ✅ `META_PHONE_NUMBER_ID` - ID номера телефона
- ✅ `META_BUSINESS_ACCOUNT_ID` - ID бизнес-аккаунта
- ✅ `META_WEBHOOK_VERIFY_TOKEN` - токен для webhook

**Проверка:** `GET /api/settings/status` должен показать `meta_api.connected: true`

### 3. Redis

**Обязательно запущен:**
```bash
brew services start redis
# или
redis-server
```

**Проверка:** Бэкенд должен запускаться без ошибок подключения к Redis

### 4. Шаблоны

**Нужно синхронизировать:**
1. Откройте страницу Templates
2. Нажмите "Синхронизировать"
3. Шаблоны загрузятся из Meta API

### 5. Контакты

**Нужно добавить контакты:**
- Через страницу Contacts
- Или импорт через `POST /api/contacts/import`
- Важно: контакты должны иметь `opt_in: true`

## 🚀 Процесс отправки рассылки

### Шаг 1: Создание кампании
1. Откройте `/broadcasts/create`
2. Выберите шаблон
3. Настройте переменные
4. Выберите получателей (contact_ids, contact_list_id, или contact_tags)
5. Настройте rate limits
6. Создайте кампанию

### Шаг 2: Запуск кампании
1. Откройте детали кампании
2. Нажмите "Start Campaign"
3. Бэкенд:
   - Создаст задачи в очереди (BullMQ)
   - Обновит статус на `running`
   - Worker начнет отправлять сообщения

### Шаг 3: Мониторинг
- Статистика обновляется в реальном времени
- Логи доступны на странице Logs
- Статусы получателей видны в деталях кампании

## ⚠️ Важные замечания

1. **Rate Limits Meta:**
   - Новые номера: 1,000 сообщений/день
   - Увеличиваются со временем
   - Бэкенд учитывает лимиты через настройки `rate_limit`

2. **24h Window:**
   - Ответить на сообщение можно только в течение 24 часов
   - После этого только шаблонные сообщения

3. **Opt-in:**
   - Отправляются только контакты с `opt_in: true`
   - Бэкенд автоматически фильтрует

4. **Webhook:**
   - Нужен для получения статусов доставки
   - Настройте в Meta Dashboard
   - URL: `https://your-backend.com/api/webhook`

## 🔧 Возможные проблемы

### Кампания не запускается
- Проверьте, что Redis запущен
- Проверьте логи бэкенда
- Убедитесь, что есть получатели со статусом `pending`

### Сообщения не отправляются
- Проверьте `META_ACCESS_TOKEN`
- Проверьте логи worker'а
- Проверьте rate limits Meta

### Ошибки в логах
- Проверьте подключение к Supabase
- Проверьте структуру таблиц
- Проверьте переменные окружения

## ✅ Итоговая готовность

- ✅ **Backend API** - готов
- ✅ **Worker** - готов
- ✅ **Frontend** - переключен на бэкенд API
- ⚠️ **Требуется:** Настройка Meta API токенов
- ⚠️ **Требуется:** Создание таблиц в Supabase
- ⚠️ **Требуется:** Синхронизация шаблонов
- ⚠️ **Требуется:** Добавление контактов

**Система готова к работе после настройки Meta API и создания таблиц!**

