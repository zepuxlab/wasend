# Инструкция: Тестовый режим для проверки кампаний без реальных вызовов Meta API

## Как использовать тестовый режим

Тестовый режим позволяет проверить работу кампаний без реальных вызовов к Meta API. Все запросы к Meta API будут мокироваться, но вся остальная логика (создание получателей, добавление в очередь, обновление статусов) работает как обычно.

### Включение тестового режима

Добавьте в файл `.env` (в корне проекта или в `backend/.env`):

```bash
META_API_TEST_MODE=true
```

### Что происходит в тестовом режиме

1. **Отправка шаблонных сообщений** (`sendTemplateMessage`):
   - Реальный запрос к Meta API **не отправляется**
   - В консоль выводится лог с префиксом `🧪 TEST MODE` и полным телом запроса
   - Возвращается мок-ответ с тестовым `message_id` (формат: `wamid.TEST_...`)
   - Сообщения сохраняются в базу данных как обычно
   - Статусы получателей обновляются как обычно

2. **Отправка текстовых сообщений** (`sendTextMessage`):
   - Аналогично шаблонным сообщениям

3. **Получение шаблонов** (`getTemplates`):
   - Возвращается пустой массив (шаблоны нужно создавать вручную в БД)

4. **Проверка подключения** (`getHealth`):
   - Возвращает `connected: true` с флагом `test_mode: true`

5. **Информация о номере телефона** (`getPhoneNumberInfo`):
   - Возвращает тестовые данные

6. **Информация о бизнес-аккаунте** (`getBusinessAccountInfo`):
   - Возвращает тестовые данные

### Как проверить работу кампании

1. **Включите тестовый режим**:
   ```bash
   # В .env файле
   META_API_TEST_MODE=true
   ```

2. **Перезапустите backend**:
   ```bash
   pm2 restart relayne-backend
   # или
   npm run dev  # для локальной разработки
   ```

3. **Создайте кампанию** через UI или API

4. **Запустите кампанию** - она будет обработана без реальных вызовов к Meta API

5. **Проверьте логи** - вы увидите сообщения с префиксом `🧪 TEST MODE`:
   ```bash
   # На сервере
   tail -f /home/ec2-user/officeamprio/wasendler/logs/backend-out.log | grep "TEST MODE"
   ```

6. **Проверьте результаты**:
   - В базе данных будут созданы записи в `messages` с тестовыми `whatsapp_message_id`
   - Статусы получателей будут обновлены (`sent`, `failed`, и т.д.)
   - Чаты будут созданы/обновлены как обычно

### Отключение тестового режима

Удалите или установите в `false` переменную `META_API_TEST_MODE`:

```bash
META_API_TEST_MODE=false
# или просто удалите строку
```

После этого перезапустите backend.

### Важно

- ⚠️ В тестовом режиме **реальные сообщения не отправляются** в WhatsApp
- ✅ Вся остальная логика работает как обычно (БД, очереди, статусы)
- ✅ Можно безопасно тестировать большие кампании без риска отправки реальных сообщений
- ✅ Логи показывают, какие запросы были бы отправлены в Meta API

### Пример логов в тестовом режиме

```
🧪 TEST MODE: Mocking Meta API call for template message
📤 Request: {
  "messaging_product": "whatsapp",
  "to": "+971501234567",
  "type": "template",
  "template": {
    "name": "welcome_message",
    "language": {
      "code": "en"
    },
    "components": [
      {
        "type": "body",
        "parameters": [
          {
            "type": "text",
            "text": "John"
          }
        ]
      }
    ]
  }
}
```

Это показывает, что именно было бы отправлено в Meta API, если бы тестовый режим был выключен.

