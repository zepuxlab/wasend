# 🚀 Быстрый старт

## Шаг 1: Получите данные Supabase

1. Перейдите на [https://supabase.com](https://supabase.com)
2. Создайте новый проект (бесплатный план)
3. После создания проекта:
   - Перейдите в **Settings** (⚙️) → **API**
   - Скопируйте:
     - **Project URL** → `SUPABASE_URL`
     - **anon public** ключ → `SUPABASE_ANON_KEY`
     - **service_role** ключ → `SUPABASE_SERVICE_KEY`

📖 Подробная инструкция: см. `SETUP_SUPABASE.md`

## Шаг 2: Настройте Backend

1. Откройте файл `backend/.env`
2. Заполните данные Supabase:

```env
SUPABASE_URL=https://ваш-проект.supabase.co
SUPABASE_SERVICE_KEY=ваш-service-role-key
SUPABASE_ANON_KEY=ваш-anon-key
```

3. Остальные переменные (Meta API) можно оставить пустыми для начала

## Шаг 3: Создайте таблицы в Supabase

1. Откройте Supabase Dashboard
2. Перейдите в **SQL Editor**
3. Создайте новый запрос
4. Скопируйте SQL схему из `frontend/src/pages/Settings.tsx` (кнопка "Копировать SQL схему")
   Или найдите SQL схему в файле `frontend/src/pages/Settings.tsx` (начинается с `CREATE EXTENSION IF NOT EXISTS "uuid-ossp";`)
5. Выполните SQL запрос

## Шаг 4: Запустите Backend

```bash
cd backend
npm install  # если еще не установлено
npm run dev
```

Сервер должен запуститься на `http://localhost:3001`

## Шаг 5: Запустите Frontend

В новом терминале:

```bash
cd frontend
bun install  # если еще не установлено
bun run dev
```

Приложение откроется на `http://localhost:8080`

## ✅ Проверка

1. Откройте http://localhost:8080
2. Приложение должно автоматически подключиться к бэкенду
3. Если видите ошибку подключения - проверьте:
   - Запущен ли бэкенд
   - Правильно ли заполнен `.env` файл
   - Созданы ли таблицы в Supabase

## 🔧 Troubleshooting

### Ошибка "Missing required environment variables"
- Проверьте, что все переменные в `backend/.env` заполнены
- Особенно: `SUPABASE_URL`, `SUPABASE_SERVICE_KEY`, `SUPABASE_ANON_KEY`

### Ошибка подключения к Supabase
- Проверьте правильность ключей
- Убедитесь, что проект Supabase активен

### CORS ошибка
- Убедитесь, что бэкенд запущен на порту 3001
- Проверьте, что фронтенд работает на порту 8080

